name: ts_schema_codegen_composed_example
description: TS schema split across multiple files with shared traits.
publish_to: none
environment:
  sdk: ^3.4.0

dev_dependencies:
  build_runner: ^2.4.15
  ts_schema_codegen:
    path: ../..
