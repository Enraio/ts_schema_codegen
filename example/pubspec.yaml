name: ts_schema_codegen_example
description: Generic dynamic-form example using the field_definitions template.
publish_to: none
environment:
  sdk: ^3.4.0

dev_dependencies:
  build_runner: ^2.4.15
  ts_schema_codegen:
    path: ..
